spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    open-in-view: true
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/reservation
    username:
    password:
    driverClassName: org.postgresql.Driver

