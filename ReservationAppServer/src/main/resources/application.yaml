spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/reservation?createDatabaseIfNotExists=true
    username:
    password:
    driverClassName: org.postgresql.Driver

